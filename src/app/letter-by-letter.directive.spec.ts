import { LetterByLetterDirective } from './letter-by-letter.directive';

describe('LetterByLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new LetterByLetterDirective();
    expect(directive).toBeTruthy();
  });
});
